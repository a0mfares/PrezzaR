import 'package:flutter/material.dart';

class GlobalBtn extends StatelessWidget {
  const GlobalBtn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
